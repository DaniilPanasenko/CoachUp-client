import {
  SET_LANGUAGE
} from '../actions/types'

const initialState = {
  isAuthorized: false,
  login: 'DaniilPanasenko'
}
export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
